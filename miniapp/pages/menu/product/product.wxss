/* pages/menu/product/product.wxss */
@import "../../../app.wxss";
page{
background-color: var(--main--gray--bg);
}

.product-swiper{
  z-index: -1;
  
}

.product-detail{
  z-index: 3;
  background-color: var(--grey-1);
  padding: 30rpx;
  border-radius: 30rpx;
}

/* 
.swiper {
  height: 450rpx;
  border-radius: 16px;
  overflow: hidden;
}

.swiper-item image {
  width: 100%;
  height: 100%;
}

.info {
  height: 600rpx;
  border-radius: 100px;
  border: 3px solid rgb(35, 151, 11);
  font-size: 20px;
  font-family: '楷体';
  font-weight: bold;
  transform: translate(0%, -8%);
  z-index: 40;
}

.info .body {
  transform: translate(8%, 5%);
}

.info text {
  font-weight: normal;
  font-size: 12px;
}

.info text:nth-of-type(odd) {
  font-weight: bold;
  font-size: 16px;
}

.hanhui {
  width: 100rpx;
  height: 100rpx;
  z-index: 100;
  position: absolute;
}

.button {
  position: absolute;
  transform: translate(960%, 15%);
  z-index: 1000;
  width: 70rpx;
  height: 70rpx;
}

.button image {
  display: flex;
  transform: translate(-42%, 0%);
  width: 70rpx;
  height: 70rpx;
}

.container {
  width: 100%;
  height: 400rpx;
  transform: translate(-3%, -10%);
  border-radius: 100rpx;
  padding: 20rpx;
}

.product-list,
.cart {
  margin-bottom: 20rpx;
  font-size: 80rpx;
}

.product-item,
.cart-item {
  display: flex;
  justify-content: space-between;
  align-items: center;
  margin-bottom: 10rpx;
}

.total {
  font-size: 50rpx;
  font-weight: bold;
  text-align: right;
}

.back image {
  width: 750rpx;
  height: 1000rpx;
}

.back {
  z-index: -10;
  transform: translate(0%, 45%);
  position: absolute;
}

.katong image {
  width: 250rpx;
  height: 250rpx;
}

.katong {
  z-index: 0;
  transform: translate(160%, 420%);
  position: absolute;
}

.custom-capsule {
  width: 100%;
  display: flex;
  align-items: center;
  justify-content: center;
}

.custom-capsule__icon {
  flex: 1;
  position: relative;
}

.custom-capsule__icon.home:before {
  content: '';
  display: block;
  position: absolute;
  left: -1px;
  top: 50%;
  transform: translateY(-50%);
  width: 1px;
  height: 18px;
  background: #e7e7e7;
} */
